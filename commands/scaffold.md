---
description: Create project files from an approved plan — .dev/ structure, packages, CLAUDE.md, git
---

This command is not yet implemented. It will:

- Create `.dev/` directory with all project files
- Write package files, STATUS.md, DECISIONS.md, STANDARDS.md
- Generate CLAUDE.md with session start protocol
- Initialize git and optionally create a GitHub repo
- Confirm handoff to Builder

Run `/pm:scaffold` to create project files. Usually called automatically by `/pm:plan`.
